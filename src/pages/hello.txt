<div className="flex justify-between">
                <StockLegend />
                <MetersTable />
                <Definitions />
            </div>

            const Definitions = () => (
    <div className="text-xs p-3 bg-white text-gray-900 rounded shadow-lg border border-gray-200 w-1/3 mr-4">
        <p className="font-bold text-gray-900 mb-1">Definitions:</p>
        {/* Using style white-space: pre-wrap to handle line breaks like in the image */}
        <pre className="whitespace-pre-wrap">
            <span className="font-semibold">PCB:</span> Printed Circuit Board{"\n"}
            <span className="font-semibold">TTB:</span> Test Terminal Board{"\n"}
            <span className="font-semibold">CT:</span> Current Transformer{"\n"}
            <span className="font-semibold">SPMs:</span> Single Phase Meters{"\n"}
            <span className="font-semibold">TPMs:</span> Three Phase Meters{"\n"}
            <span className="font-semibold">CIU:</span> Customer Interface Units
        </pre>
    </div>
  );
  
  const MetersTable = () => (
    <div className="bg-white p-3 rounded shadow-lg w-1/3">
        <h2 className="font-bold mb-2 text-gray-900 text-lg border-b pb-1">Meters</h2>
        <div className="grid grid-cols-2 text-gray-900 text-sm gap-y-1">
            {/* Total Coupled/Sent on top with bold values */}
            <p className="font-semibold ">Total Coupled</p>
            <p className="text-right font-bold">{metersData.totalCoupled.toLocaleString()}</p>
            <p className="font-semibold">Total Sent</p>
            <p className="text-right font-bold">{metersData.totalSent.toLocaleString()}</p>
            
            {/* SPM/TPM/CTI below, slightly less emphasis */}
            <p className="mt-2">SPMs:</p>
            <p className="text-right mt-2">{metersData.spms.toLocaleString()}</p>
            <p>TPMs:</p>
            <p className="text-right">{metersData.tpms.toLocaleString()}</p>
            <p>CTIs:</p>
            <p className="text-right">{metersData.ctis.toLocaleString()}</p>
        </div>
    </div>
  );

  const metersData = {
    totalCoupled: 3000,
    totalSent: 1000,
    spms: 3000,
    tpms: 2300,
    ctis: 1000,
  };